import { createStyles } from "antd-style";

const useAoVideoConvertStyles = createStyles(({ token, css }) => {
  return {
    aoPageVideoConvert: css`
      width: 100;
      height: 100%;
    `
  }
})

export default useAoVideoConvertStyles;